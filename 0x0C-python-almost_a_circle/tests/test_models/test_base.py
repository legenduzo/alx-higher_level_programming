#!/usr/bin/python3
import unittest

class TestBase(unittest.TestCase):

    def test_id:
        t = Base(5)
        self.assertEqual(t.id, 5)

    def test_noid:
        t = Base()
        self.assertEqual(t.id, 1)


if __name__ == '__main__':
    unittest.main()
